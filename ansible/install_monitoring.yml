---
- name: Deploy Centralized Prometheus and Grafana Stack through Docker
  hosts: all
  become: true

  tasks:
    - name: Create configuration directory
      ansible.builtin.file:
        path: /etc/prometheus
        state: directory
        mode: "0755"
      when: inventory_hostname == play_hosts[4]

    - name: Generate centralized prometheus.yml config
      ansible.builtin.copy:
        dest: /etc/prometheus/prometheus.yml
        mode: "0644"
        content: |
          global:
            scrape_interval: 15s
          scrape_configs:
            - job_name: 'production_app_cluster'
              static_configs:
                - targets:
                    - '{{ play_hosts[0] }}:9100'
                    - '{{ play_hosts[1] }}:9100'
                    - '{{ play_hosts[2] }}:9100'
                    - '{{ play_hosts[3] }}:9100'
      when: inventory_hostname == play_hosts[4]

    - name: Launch Prometheus
      ansible.builtin.command: >
        docker run -d --name prometheus --restart always -p 9090:9090
        -v /etc/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
        prom/prometheus:latest
      when: inventory_hostname == play_hosts[4]
      changed_when: false

    - name: Launch Grafana
      ansible.builtin.command: >
        docker run -d --name grafana --restart always -p 3000:3000
        grafana/grafana:latest
      when: inventory_hostname == play_hosts[4]
      changed_when: false
